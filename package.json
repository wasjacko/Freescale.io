{
  "name": "freescale",
  "version": "0.1.0",
  "private": true,
  "description": "Unified multi-channel inbox with AI copilot Mue",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "test": "turbo test",
    "format": "biome format --write .",
    "format:check": "biome format .",
    "db:types": "bash -c 'echo \"// @ts-nocheck — auto-generated by supabase gen types\" > apps/web/lib/supabase/database.types.ts && supabase gen types typescript --project-id zskvdvybgillldwjtvvi --schema public >> apps/web/lib/supabase/database.types.ts'",
    "db:push": "supabase db push",
    "db:new": "supabase migration new"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.0",
    "turbo": "^2.3.0",
    "typescript": "^5.6.0"
  },
  "workspaces": ["apps/*", "packages/*"]
}
